// This is an working example that has an article that 
// uses the citation hyperlinked to showcase the example
// when user clicks --> new tab whereby showing if the link is valid
// citation of the parent reference material

const BlogReference = () => {
  return <div>
    <h1>Example showing our Dapp</h1>
    
  </div>
}

export default BlogReference;